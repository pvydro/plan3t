export interface IInterpolationManager {

}

export class InterpolationManager implements IInterpolationManager {
    constructor() {

    }

    // Either this, or client side entities handle this within themselves.
}