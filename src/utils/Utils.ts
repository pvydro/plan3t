export function exists(item): boolean {
    let doesExist: boolean = true
    
    if (item == null || item == undefined) {
        doesExist = false
    }

    return doesExist
}

export class Utils {

}
