import { PlayerHairType } from '../../../../model/PlayerCustomizationTypes'
import { PlayerHair } from '../PlayerHair';

export class PlayerHairFactory {
    static createHairForType(type: PlayerHairType) {

        // switch (type) {
            
        // }
    }
}
