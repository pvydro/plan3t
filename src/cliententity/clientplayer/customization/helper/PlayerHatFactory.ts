import { PlayerHatType } from '../../../../model/PlayerCustomizationTypes'

export class PlayerHatFactory {
    static createHatForType(type: PlayerHatType) {
        switch (type) {
            
        }
    }
}
