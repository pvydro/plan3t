export enum PlayerBodyState {
    Idle = 0,
    Walking = 1
}

export enum PlayerLegsState {
    Standing = 0,
    Crouched = 1,
    Jumping = 2
}

export enum Direction {
    Left = -1,
    Right = 1
}
