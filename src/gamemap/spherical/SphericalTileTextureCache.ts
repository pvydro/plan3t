export interface ISphericalTileTextureCache {

}

export class SphericalTileTextureCache {
    // static map with textures based on tilesheetURL and coordsx, y.
    // Combines tilesheetURL string with x and y to apply a string key to the cached cropped tile texture
    // Return the texture (base64 string) if found

    private constructor() {}
}
